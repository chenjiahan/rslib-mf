{
  "name": "rslib-mf",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rslib build",
    "check": "biome check --write",
    "dev": "rslib build --watch",
    "format": "biome format --write"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.8.3",
    "@rslib/core": "^0.0.5",
    "typescript": "^5.5.4"
  }
}
